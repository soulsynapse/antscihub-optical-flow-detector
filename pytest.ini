[pytest]
# Real tests live under tests/. Scoping collection here keeps pytest from
# importing dev scripts and shelved modules (e.g. scripts/_shelved/*_test.py),
# which are not tests and may reference intentionally-removed code.
testpaths = tests
